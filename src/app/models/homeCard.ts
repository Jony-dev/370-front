export class HomeCard {

    table : { tableName : string};
    assignedCards : { total : number};
    jobRequests : { total : number};
    jobApprovals : { total : number};
    interviews : { total : number};
}